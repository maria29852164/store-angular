import { NumberCharacterPipe } from './number-character.pipe';

describe('NumberCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
