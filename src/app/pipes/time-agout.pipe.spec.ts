import { TimeAgoutPipe } from './time-agout.pipe';

describe('TimeAgoutPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoutPipe();
    expect(pipe).toBeTruthy();
  });
});
